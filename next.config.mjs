/** @type {import('next').NextConfig} */
/** this is a Next.js configuration file, it was ensembled by G_Group */
const nextConfig = {
  images: {
    remotePatterns: [{ hostname: "res.cloudinary.com" }],
  
  },
};

export default nextConfig;
